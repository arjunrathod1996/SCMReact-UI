import { useCallback } from "react";

export const useDataIntl = () => {
    
    const getLocalData = useCallback(
        ()=>{
            //
        },[]
    );
    return {
        getLocalData,
    }
}